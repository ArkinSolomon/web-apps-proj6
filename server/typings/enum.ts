export enum UserRole {
  Faculty = 'faculty',
  Student = 'student'
}
export enum TermSeason {
  Spring = 'spring',
  Summer = 'summer',
  Fall = 'fall'
}
export enum AccomplishmentType {
  Major = 'major',
  Minor = 'minor'
}
export enum RequirementType {
  Core = 'core',
  Elective = 'elective',
  Cognate = 'cognate' // I still don't know what a cognate is lol
}